# Implementation Plan

- [x] 1. Set up feature module structure and types
  - [x] 1.1 Create schedule feature directory structure
    - Create `packages/web/src/features/schedule/` directory
    - Create subdirectories: `__tests__/`, `hooks/`, `stores/`, `utils/`
    - Create `index.ts` for public exports
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_
  - [x] 1.2 Implement TypeScript types (types.ts)
    - Define DayOfWeek enum matching solver values
    - Define ScheduledLesson interface with all fields
    - Define ClassMetadata, SubjectMetadata, TeacherMetadata, RoomMetadata
      interfaces
    - Define PeriodConfiguration and SolutionMetadata interfaces
    - Define SolutionStatistics interface with all statistical fields
    - Define ScheduleIndexes interface with Map types
    - Define DisplaySettings interface
    - Define ScheduleState, TimetableApiResponse, NormalizedSchedule types
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_
  - [x] 1.3 Implement constants (constants.ts)
    - Define DAYS_OF_WEEK array with Persian/English labels
    - Define GRADE_CATEGORIES object with grade ranges and labels
    - Define CONSTRAINT_TYPES enum
    - Define DEFAULT_DISPLAY_SETTINGS
    - _Requirements: 6.1, 6.2, 6.3, 6.4_

- [x] 2. Implement index builder utility
  - [x] 2.1 Create indexBuilder.ts with buildIndexes function
    - Implement slot key generation: `${day}-${periodIndex}`
    - Implement entity-slot key generation: `${entityId}-${day}-${periodIndex}`
    - Implement bySlot index population
    - Implement byTeacherAndSlot with multi-teacher support
    - Implement byRoomAndSlot index (skip null roomIds)
    - Implement byClassAndSlot index
    - Implement byTeacher, byClass, byRoom grouping indexes
    - Handle empty lessons array edge case
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_
  - [x] 2.2 Write property test: Index Builder Completeness
    - **Property 1: Index Builder Completeness**
    - **Validates: Requirements 3.2, 3.3, 3.5**
    - Create scheduledLessonArb generator
    - Test every lesson findable via bySlot, byClassAndSlot, byClass, byTeacher,
      byRoom
  - [x] 2.3 Write property test: Multi-Teacher Index Correctness
    - **Property 2: Multi-Teacher Index Correctness**
    - **Validates: Requirements 3.4**
    - Create multi-teacher lesson generator
    - Test each teacherId has entry in byTeacherAndSlot
  - [x] 2.4 Write unit tests for index builder edge cases
    - Test empty lessons array returns empty Maps
    - Test single lesson indexed correctly
    - Test lesson with null roomId not in byRoom
    - _Requirements: 3.6_

- [x] 3. Implement schedule transformer utility
  - [x] 3.1 Create scheduleTransformer.ts with normalizeSchedule function
    - Parse JSON data field from API response
    - Map schedule array items to ScheduledLesson objects
    - Extract and structure metadata (classes, subjects, teachers,
      periodConfiguration)
    - Map statistics fields to SolutionStatistics
    - Implement error handling for malformed JSON
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6_
  - [x] 3.2 Write property test: Transformer Round-Trip Consistency
    - **Property 3: Transformer Round-Trip Consistency**
    - **Validates: Requirements 4.2, 4.3, 4.4, 4.5**
    - Create NormalizedSchedule generator
    - Test serialize then normalize produces equivalent data
  - [x] 3.3 Write unit tests for transformer
    - Test valid API response transforms correctly
    - Test malformed JSON throws descriptive error
    - Test missing optional fields handled gracefully
    - _Requirements: 4.6_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 5. Implement Zustand store
  - [x] 5.1 Create scheduleStore.ts with state and actions
    - Define initial state with all required fields
    - Implement loadSchedule action with async fetch, transform, index build
    - Implement clearSchedule action to reset state
    - Implement updateIndexes action
    - Implement setDisplaySettings action
    - Use immer middleware for immutable updates
    - Implement error handling in loadSchedule
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6_
  - [x] 5.2 Write property test: Store Clear Reset Invariant
    - **Property 4: Store Clear Reset Invariant**
    - **Validates: Requirements 2.3**
    - Generate random store states
    - Test clearSchedule always returns to initial state
  - [x] 5.3 Write property test: Store Index Consistency
    - **Property 5: Store Index Consistency**
    - **Validates: Requirements 2.4**
    - Generate random lessons arrays
    - Test updateIndexes produces consistent indexes
  - [x] 5.4 Write unit tests for store
    - Test initial state has correct structure
    - Test loadSchedule error handling sets error field
    - Test clearSchedule resets to initial state
    - _Requirements: 2.1, 2.6_

- [x] 6. Implement API layer and TanStack Query hooks
  - [x] 6.1 Create api.ts with schedule API functions
    - Implement getById function
    - Implement getAll function
    - Implement save function
    - Implement delete function
    - Add logging using existing logger pattern
    - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - [x] 6.2 Create useSchedule.ts with TanStack Query hooks
    - Implement useSchedule hook with query key ['schedule', id]
    - Implement useSchedules hook with query key ['schedules']
    - Implement useSaveSchedule mutation with cache invalidation
    - Implement useDeleteSchedule mutation with cache invalidation
    - Add Farsi toast notifications for success/error
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.7_
  - [x] 6.3 Write unit tests for hooks
    - Test useSchedule returns transformed data
    - Test mutations invalidate correct query keys
    - _Requirements: 5.5_

- [x] 7. Create feature module exports and documentation
  - [x] 7.1 Update index.ts with public exports
    - Export all types
    - Export constants
    - Export hooks
    - Export store
    - Export utility functions
    - _Requirements: All_
  - [x] 7.2 Create utils/logger.ts following teachers feature pattern
    - Create logger instance for schedule feature
    - Create apiLogger for API call logging
    - _Requirements: All_

- [ ] 8. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
